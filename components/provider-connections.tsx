"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
import { CheckCircle2, KeyRound, LoaderCircle, RefreshCw, Unplug } from "lucide-react";

type Provider="openai"|"anthropic"|"google"|"ollama";
type Model={id:string;name:string};
type Connection={provider:Provider;keyHint:string;selectedModel:string;models:Model[];active:boolean;lastCheckedAt?:string};
const providers:{id:Provider;name:string;copy:string;key:boolean}[]=[
  {id:"openai",name:"OpenAI",copy:"GPT and compatible models available to your account.",key:true},
  {id:"anthropic",name:"Claude",copy:"Claude models enabled for your Anthropic workspace.",key:true},
  {id:"google",name:"Gemini",copy:"Gemini models supporting generateContent.",key:true},
  {id:"ollama",name:"Ollama",copy:"Models installed on your configured Ollama server.",key:false},
];

export function ProviderConnections(){const [connections,setConnections]=useState<Connection[]>([]),[busy,setBusy]=useState<Provider|null>(null),[notice,setNotice]=useState("");const load=useCallback(async()=>{const r=await fetch("/api/providers");if(r.ok)setConnections((await r.json()).connections)},[]);useEffect(()=>{void load()},[load]);
 async function connect(event:FormEvent<HTMLFormElement>,provider:Provider){event.preventDefault();setBusy(provider);setNotice("");const data=new FormData(event.currentTarget);const r=await fetch("/api/providers",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"connect",provider,apiKey:data.get("apiKey")||undefined})});const body=await r.json();if(r.ok){event.currentTarget.reset();setNotice(`${providers.find(x=>x.id===provider)?.name} connected. ${body.connection.models.length} models found.`);await load()}else setNotice(body.error||"Connection failed.");setBusy(null)}
 async function refresh(provider:Provider){setBusy(provider);const r=await fetch("/api/providers",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"refresh",provider})});const body=await r.json();setNotice(r.ok?`Model list refreshed. ${body.connection.models.length} models available.`:body.error||"Refresh failed.");if(r.ok)await load();setBusy(null)}
 async function select(provider:Provider,model:string){setBusy(provider);const r=await fetch("/api/providers",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"select",provider,model})});setNotice(r.ok?"Default model updated.":"That model could not be selected.");if(r.ok)await load();setBusy(null)}
 async function disconnect(provider:Provider){setBusy(provider);await fetch(`/api/providers?provider=${provider}`,{method:"DELETE"});await load();setNotice(`${providers.find(x=>x.id===provider)?.name} disconnected.`);setBusy(null)}
 return <section className="lg:col-span-2 rounded-3xl border-2 border-ink bg-white p-6 shadow-[0_5px_0_#26312c]"><div className="flex items-start gap-3"><div className="grid size-11 place-items-center rounded-2xl bg-coral"><KeyRound/></div><div><h2 className="font-[var(--font-display)] text-xl font-black">AI provider connections</h2><p className="mt-1 text-sm text-ink/55">Keys are encrypted before storage. Connecting verifies the key and asks the provider for models currently available to it.</p></div></div>{notice&&<p role="status" className="mt-4 rounded-2xl bg-mint/25 px-4 py-3 text-sm font-bold">{notice}</p>}<div className="mt-6 grid gap-4 md:grid-cols-2">{providers.map(provider=>{const connection=connections.find(x=>x.provider===provider.id&&x.active);return <article key={provider.id} className={`rounded-3xl border-2 p-5 ${connection?"border-mint bg-mint/10":"border-ink/15 bg-cream/35"}`}><div className="flex items-start justify-between"><div><div className="flex items-center gap-2"><h3 className="font-black">{provider.name}</h3>{connection&&<span className="flex items-center gap-1 rounded-full bg-mint px-2 py-1 text-[10px] font-black uppercase"><CheckCircle2 size={12}/>Active</span>}</div><p className="mt-1 text-xs leading-5 text-ink/50">{provider.copy}</p></div>{connection&&<button onClick={()=>disconnect(provider.id)} disabled={busy===provider.id} title="Disconnect"><Unplug size={17} className="text-ink/35 hover:text-coral"/></button>}</div>{connection?<div className="mt-4"><div className="flex items-end gap-2"><label className="min-w-0 flex-1 text-[10px] font-black uppercase tracking-wider text-plum">Model<select value={connection.selectedModel} onChange={e=>select(provider.id,e.target.value)} disabled={busy===provider.id} className="mt-1 w-full rounded-xl border-2 border-ink/15 bg-white px-3 py-2.5 text-sm font-bold normal-case"><option value="" disabled>Select a model</option>{connection.models.map(model=><option key={model.id} value={model.id}>{model.name}</option>)}</select></label><button onClick={()=>refresh(provider.id)} disabled={busy===provider.id} className="grid size-10 place-items-center rounded-xl border-2 border-ink/15 bg-white" title="Refresh models">{busy===provider.id?<LoaderCircle className="animate-spin" size={16}/>:<RefreshCw size={16}/>}</button></div><p className="mt-2 text-xs text-ink/40">{connection.keyHint} · {connection.models.length} models discovered</p></div>:<form onSubmit={e=>connect(e,provider.id)} className="mt-4">{provider.key&&<input name="apiKey" type="password" required autoComplete="off" placeholder={`${provider.name} API key`} className="w-full rounded-xl border-2 border-ink/15 bg-white px-3 py-2.5 text-sm outline-none focus:border-coral"/>}<button disabled={busy===provider.id} className="mt-2 flex w-full items-center justify-center gap-2 rounded-xl border-2 border-ink bg-sun py-2.5 text-sm font-black shadow-[0_3px_0_#26312c]">{busy===provider.id?<LoaderCircle className="animate-spin" size={16}/>:<KeyRound size={16}/>}Connect & discover models</button></form>}</article>})}</div></section>}
